# Required: read-only DatoCMS content-delivery API token (used at build time).
DATOCMS_API_KEY=

# Optional: PostHog analytics. If unset, the values baked into
# src/components/Posthog.astro are used as a fallback.
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=
